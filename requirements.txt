# Web-UI
# streamlit >= 1.49: width="stretch" (loest das abgekuendigte
# use_container_width ab).
streamlit>=1.49
streamlit_folium>=0.24
streamlit-javascript

# Datenhaltung
duckdb>=1.0
pandas>=2.0

# GPX-Verarbeitung & Geodaten
gpxpy
geopandas>=1.0
pyogrio

# Geocoding & Zeitzone
geopy
timezonefinder

# Karte (Heatmap auf der Statistik-Seite, siehe stats.py; die Kartenseite
# "Karte" selbst laedt Leaflet/uPlot per CDN im Browser, siehe map_linked.py)
folium>=0.16
